require 'spec_helper'

describe OnionRing do
  it 'has a version number' do
    expect(OnionRing::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
